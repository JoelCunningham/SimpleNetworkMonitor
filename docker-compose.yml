version: "3.8"

services:
  monolith:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - CONFIG__DEBUG=false
    volumes:
      - ./:/app
    depends_on: []

  # Example additional service scaffold (commented out until implemented)
  # scan_api:
  #   build: ./services/scan_api
  #   ports:
  #     - "8100:8000"
  #   environment:
  #     - PYTHONUNBUFFERED=1
  #   volumes:
  #     - ./services/scan_api:/app
